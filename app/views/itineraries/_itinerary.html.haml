- if (params['controller'] == 'itineraries') and (template = template_for_current_view).present?
  = render(template, :itinerary => itinerary)
- else
  -# default view
  = render 'itineraries/index_long', :itinerary => itinerary
