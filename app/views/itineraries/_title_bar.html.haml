#confirm{ hidden }
.title_tools#menu
  = link_to_inline(:edit_itinerary, edit_itinerary_path(@itinerary), :text => t(:edit)) + " | "
  = link_to_function(t(:delete) + '?', confirm_delete(@itinerary))
.title_tools#buttons
  = view_buttons
.title#edit_itinerary_title
  = h(@itinerary.name)
  = image_tag("loading.gif", :size => :thumb, :id => "loading", :style => "display: none;")

%div#edit_itinerary{ hidden }
