= styles_for :campaign

= render 'entities/title_bar'

.remote#create_campaign{ hidden }

= render 'search'

= simple_table_for @campaigns, with: CampaignTable

.remote#options{ hidden }

.list#campaigns
  - if @campaigns.any?
    = render @campaigns
  - else
    = render "shared/empty"

#paginate= render "shared/paginate_with_per_page"
#export= render "shared/export"
